%div.content

  %h1 Settings
  
  %p
    You are currently signed in using your 
    = link_to(current_user.nickname, current_user.personal_homepage)
    = current_user.provider.titlecase
    account.

  = semantic_form_for(current_user, :html => {:class => ""}) do |form|
    = errors_for form
    
    = form.inputs do
      = form.input :mode, :as => :select, :collection => %w(public private), :include_blank => false, :hint => t('formtastic.hints.user.mode')

    = form.buttons do
      = form.submit "Save"