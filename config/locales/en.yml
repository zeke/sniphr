# Sample localization file for English. Add more files in this directory for other locales.
# See http://github.com/svenfuchs/rails-i18n/tree/master/rails%2Flocale for starting points.

en:
  hello: "Hello world"
  
  activerecord:
    model:
      user: "User"
    attributes:
      user:
        whitelist: "Domain Whitelist"

  formtastic:
    hints:
      user:
        mode: "Sniphs saved when you're in private mode are only visible to you. Sniphs saved when you're in public mode are visible to everyone."
        whitelist: "When you select text on pages with a whitelisted domain, it gets sniph'd automatically. One domain per line. Leave off the www."
        